# Linux 服务器日志查看指南

## 项目信息
- 项目目录: `/var/www/MyTravelPanel`
- 运行方式: Gunicorn (端口 8000)
- Web服务器: Nginx

---

## 1. 查看 Gunicorn 应用日志

### 方法1: 查看 Gunicorn 标准输出/错误（如果使用 systemd）
```bash
# 如果使用 systemd 管理 gunicorn
sudo journalctl -u gunicorn -f
# 或查看最近100行
sudo journalctl -u gunicorn -n 100
```

### 方法2: 查看 Gunicorn 进程输出
```bash
# 查找 gunicorn 进程
ps aux | grep gunicorn

# 如果 gunicorn 有日志文件（通常在项目目录下）
cd /var/www/MyTravelPanel
ls -la logs/  # 查看是否有日志目录
tail -f logs/gunicorn.log  # 实时查看日志
```

### 方法3: 查看系统日志中的 Python 错误
```bash
# 查看系统日志中的 Python 相关错误
sudo journalctl -f | grep -i python
sudo journalctl -f | grep -i error
sudo journalctl -f | grep -i traceback
```

---

## 2. 查看 Nginx 错误日志

```bash
# Nginx 错误日志通常在这里
sudo tail -f /var/log/nginx/error.log

# 或者查看访问日志
sudo tail -f /var/log/nginx/access.log

# 查看最近的错误
sudo tail -n 100 /var/log/nginx/error.log | grep -i error
```

---

## 3. 查看应用日志文件（如果配置了文件日志）

```bash
cd /var/www/MyTravelPanel

# 查找日志文件
find . -name "*.log" -type f
find . -name "*error*" -type f
find . -name "*debug*" -type f

# 查看项目目录下的日志
ls -la logs/ 2>/dev/null || echo "logs 目录不存在"
ls -la *.log 2>/dev/null || echo "当前目录无日志文件"
```

---

## 4. 实时监控错误（推荐）

### 创建一个监控脚本
```bash
# 在项目目录创建监控脚本
cd /var/www/MyTravelPanel
cat > monitor_errors.sh << 'EOF'
#!/bin/bash
echo "=== 监控应用错误 ==="
echo "按 Ctrl+C 停止监控"
echo ""

# 监控多个日志源
(
  # Gunicorn 日志（如果存在）
  if [ -f logs/gunicorn.log ]; then
    tail -f logs/gunicorn.log | grep --color=always -i "error\|exception\|traceback\|failed" &
  fi
  
  # Nginx 错误日志
  sudo tail -f /var/log/nginx/error.log | grep --color=always -i "error\|failed" &
  
  # 系统日志中的 Python 错误
  sudo journalctl -f | grep --color=always -i "python\|gunicorn\|error\|exception" &
  
  wait
) 2>/dev/null
EOF

chmod +x monitor_errors.sh
./monitor_errors.sh
```

---

## 5. 查看邮件发送相关错误

```bash
# 查看包含邮件相关错误的日志
cd /var/www/MyTravelPanel

# 在系统日志中搜索邮件相关错误
sudo journalctl -f | grep -i "mail\|email\|smtp"

# 在应用日志中搜索
grep -r -i "邮件发送失败\|mail.*error\|smtp" logs/ 2>/dev/null
```

---

## 6. 查看最近的错误（综合方法）

```bash
# 查看最近1小时的所有错误
cd /var/www/MyTravelPanel

# 系统日志
sudo journalctl --since "1 hour ago" | grep -i "error\|exception\|traceback" | tail -50

# Nginx 错误
sudo tail -100 /var/log/nginx/error.log | grep -i error

# 应用日志文件
find . -name "*.log" -type f -exec tail -50 {} \; 2>/dev/null | grep -i "error\|exception"
```

---

## 7. 检查 Gunicorn 运行状态

```bash
# 检查 gunicorn 进程
ps aux | grep gunicorn

# 检查端口占用
netstat -tlnp | grep 8000
# 或
ss -tlnp | grep 8000

# 检查 gunicorn 是否能正常响应
curl http://127.0.0.1:8000/health 2>&1
```

---

## 8. 查看特定时间的错误

```bash
# 查看今天的所有错误
sudo journalctl --since today | grep -i error

# 查看指定时间段的错误
sudo journalctl --since "2024-01-01 00:00:00" --until "2024-01-01 23:59:59" | grep -i error

# 查看最近10分钟的错误
sudo journalctl --since "10 minutes ago" | grep -i error
```

---

## 9. 快速诊断命令（一键执行）

```bash
cd /var/www/MyTravelPanel

echo "=== 1. Gunicorn 进程状态 ==="
ps aux | grep gunicorn | grep -v grep

echo ""
echo "=== 2. 最近系统错误（最后20行）==="
sudo journalctl -n 20 | grep -i "error\|exception" | tail -10

echo ""
echo "=== 3. Nginx 最近错误 ==="
sudo tail -20 /var/log/nginx/error.log | grep -i error

echo ""
echo "=== 4. 项目目录日志文件 ==="
find . -maxdepth 2 -name "*.log" -type f 2>/dev/null | head -5

echo ""
echo "=== 5. 端口监听状态 ==="
netstat -tlnp | grep 8000 || ss -tlnp | grep 8000
```

---

## 10. 针对邮件发送错误的专门查看

```bash
# 实时监控邮件发送相关错误
cd /var/www/MyTravelPanel

# 方法1: 查看系统日志中的邮件错误
sudo journalctl -f | grep --color=always -E "邮件|mail|smtp|MAIL_|邮件发送"

# 方法2: 在应用日志中搜索
grep -r "邮件发送失败\|邮件发送配置检查\|MAIL_" logs/ 2>/dev/null | tail -20

# 方法3: 查看包含邮件配置的日志
sudo journalctl | grep -E "MAIL_SERVER|MAIL_USERNAME|邮件服务器" | tail -20
```

---

## 常见问题排查

### 问题1: 找不到日志文件
```bash
# 检查 gunicorn 是否配置了日志文件
ps aux | grep gunicorn | grep -o "\\-\\-log-file[= ]\\S*"

# 如果没有配置，日志可能在系统日志中
sudo journalctl -u gunicorn
```

### 问题2: 权限不足
```bash
# 如果无法查看日志，可能需要 sudo
sudo tail -f /var/log/nginx/error.log
sudo journalctl -f
```

### 问题3: 日志文件太大
```bash
# 查看日志文件大小
du -sh /var/log/nginx/*.log
du -sh logs/*.log 2>/dev/null

# 清理旧日志（谨慎操作）
sudo journalctl --vacuum-time=7d  # 只保留7天
```

---

## 推荐工作流程

1. **快速查看**: 使用 `sudo journalctl -f` 实时监控
2. **定位错误**: 使用 `grep -i error` 过滤错误信息
3. **查看详情**: 查看完整的错误堆栈信息
4. **保存日志**: 将重要错误保存到文件以便分析

```bash
# 保存错误日志到文件
sudo journalctl --since "1 hour ago" | grep -i error > /tmp/errors_$(date +%Y%m%d_%H%M%S).log
```

---
description: 
globs: 
alwaysApply: false
---
# 项目分析

## 项目概述
这是一个基于 Flask 的旅游管理系统，主要功能包括签证管理、机票预订、旅游套餐管理等。项目采用模块化设计，使用蓝图（Blueprint）组织不同功能模块。

## 核心功能模块

### 1. 签证管理模块
- 基础信息管理 (`visa_basic_info.py`)
- 文档管理 (`visa_documents.py`)
- 文件管理 (`visa_files.py`)
- 项目管理 (`visa_project.py`)
- 链接管理 (`visa_links.py`)

### 2. 机票管理模块
- 首页管理 (`flights_home_routes.py`)
- 航班时刻表 (`flights_schedule.py`)
- 机票订单管理 (`flights_booking_routes.py`)
- AAthina订单管理 (`flights_athina_routes.py`)

### 3. 旅游套餐模块
- 套餐管理 (`tour_package.py`)
- 产品详情 (`tour_product_details.py`)
- 供应商管理 (`supplier.py`)

### 4. 项目管理模块
- 项目基础管理 (`project.py`)
- 业务类型管理 (`business_type.py`)

### 5. 工具模块
- 公司信息管理 (`utils_company_info.py`)
- 任务管理 (`utils_tasks.py`)
- 通用工具 (`utils.py`)

### 6. 账户管理
- 用户账户管理 (`account.py`)
- 报表管理 (`statement.py`)

## 技术架构

### 1. 后端框架
- Flask Web 框架
- Flask-SQLAlchemy 数据库 ORM
- Flask-Migrate 数据库迁移
- Flask-Login 用户认证
- Flask-Cache 缓存支持

### 2. 数据模型
主要模型包括：
- User - 用户模型
- Project - 项目模型
- BusinessType - 业务类型模型
- Supplier - 供应商模型

### 3. 系统特性
- 模块化设计，使用蓝图分离不同功能
- 支持数据库迁移
- 实现了用户认证系统
- 包含缓存机制
- 支持后台任务（如提醒服务）
- 静态文件管理
- 支持 .well-known 目录（可能用于应用验证）

## 开发建议

### 1. 代码组织
- 遵循模块化原则，保持功能模块的独立性
- 使用蓝图组织路由
- 将业务逻辑放在 services 目录
- 将工具函数放在 utils 目录

### 2. 安全性
- 使用环境变量管理敏感配置
- 实现完整的用户认证和授权
- 注意数据验证和清理

### 3. 性能优化
- 合理使用缓存
- 优化数据库查询
- 使用异步任务处理耗时操作

### 4. 维护性
- 保持代码注释的完整性
- 遵循 PEP 8 编码规范
- 编写单元测试
- 使用版本控制管理代码

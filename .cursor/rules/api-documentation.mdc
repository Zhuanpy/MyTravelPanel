---
description:
globs:
alwaysApply: false
---
# API 文档

## 主要路由
所有 API 路由都定义在 `App/routes/` 目录下。

### 用户相关
- 用户注册：`POST /auth/register`
- 用户登录：`POST /auth/login`
- 用户登出：`POST /auth/logout`

### 数据操作
- 获取数据：`GET /api/data`
- 创建数据：`POST /api/data`
- 更新数据：`PUT /api/data/<id>`
- 删除数据：`DELETE /api/data/<id>`

## 响应格式
所有 API 响应都遵循以下格式：
```json
{
    "code": 200,
    "message": "操作成功",
    "data": {}
}
```

## 错误处理
- 400: 请求参数错误
- 401: 未授权
- 403: 禁止访问
- 404: 资源不存在
- 500: 服务器内部错误

## 认证
大多数 API 需要认证，请在请求头中添加：
```
Authorization: Bearer <token>
```
